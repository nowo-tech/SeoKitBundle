# PHPStan configuration for Seo Kit Bundle
# Compatible with PHP 8.2+ and Symfony 7.x / 8.x
# Extensions (phpstan-symfony, phpstan-phpunit) are auto-included via phpstan/extension-installer.

parameters:
    level: 8
    paths:
        - src
        - tests
    excludePaths:
        - demo/*
